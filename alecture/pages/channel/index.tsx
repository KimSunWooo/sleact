import WorkSpace from '@layouts/Workspace';
import React from 'react';

const Channel = () => {
  return (
    <WorkSpace>
      <div>로그인 되었습니다.</div>
    </WorkSpace>
  );
};

export default Channel;
